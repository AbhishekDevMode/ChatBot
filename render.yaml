services:
  - type: web
    name: mern-chatbot
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_CONNECT
        sync: false
      - key: JWT_SECRET
        generateValue: true
